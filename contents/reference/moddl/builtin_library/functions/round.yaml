function:
  name: round
  desc: |
    与えられた数値に最も近い整数を求めます。

  typeParams:
    -
      name: Value
      type: Number | ModuleDef

  params:
    -
      name: '`arg`'
      type: Value
      required: true
      # default:
      #   value: 0
      desc: |
        任意の数値。

  value:
    type: Value
    desc: |
      `arg` に最も近い整数。

      `arg` の小数部がちょうど 0.5 の場合は、0 から遠い側の整数となります。

  details:
    - |
      この関数は Number と ModuleDef に対してオーバーロードされています。詳細は「%link(/reference/moddl/operators/overloaded_calculations)」を参照してください。
