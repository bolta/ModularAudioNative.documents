mmlCommand:
  name: |
    l, L
  desc: |
    デフォルト音長を設定します。
  format: |
    (`l`|`L`)\<length>
  params:
    -
      name: length 
      type: Number
      required: true
      desc: 音長を指定します。

  details: |
    音符・休符の音長を省略した場合に割り当たる音長を設定します。

    length に指定できるのは 0 より大きい整数値のみです。付点やタイを使うことはできません。

    `l` コマンドを一度も使っていない状態では、`l4` が指定されているものと見なされます。

    大文字の `L` を使うこともできます（意味は同じです）。
